import { queries } from "./qurey";
import { mutatsion } from "./mutatsion";
import { resolvers } from "./resolver";
import { typeDefs } from "./typedef";


//bound in obj

export const post = {queries, mutatsion , resolvers , typeDefs}