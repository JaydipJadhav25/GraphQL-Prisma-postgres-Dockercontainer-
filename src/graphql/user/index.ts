import { queries } from "./qurey";
import { mutatsion } from "./mutations";
import  {typeDefs}  from "./typeDef"
import { resolvers } from "./resolver";

export const user = { queries, mutatsion , typeDefs, resolvers}